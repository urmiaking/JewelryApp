@using JewelryApp.Shared.Enums
@using JewelryApp.Client.ViewModels.Populated
@inherits UserComponentBase

<MudDialog>
    <DialogContent>
        <EditForm Model="@Model" OnValidSubmit="OnValidSubmit">
            <DataAnnotationsValidator />
            <MudGrid>
                <MudItem xs="12" md="6" lg="6">
                    <MudTextField Label="نام جنس" @bind-Value="Model.Name" For="@(() => Model.Name)" Variant="Variant.Outlined" Margin="Margin.Dense" />
                </MudItem>
                <MudItem xs="12" md="6" lg="6">
                    <MudNumericField Label="وزن" @bind-Value="Model.Weight" For="@(() => Model.Weight)" Variant="Variant.Outlined" Margin="Margin.Dense" />
                </MudItem>
                <MudItem xs="12" md="6" lg="6">
                    <MudSelect T="WageTypeVm" Label="نوع اجرت" For="() => Model.WageTypeVm" Margin="Margin.Dense" Variant="Variant.Outlined">
                        @foreach (var wageType in WageTypeVm.GetWageTypes())
                        {
                            <MudSelectItem T="WageTypeVm" Value="wageType">@wageType.Name</MudSelectItem>
                        }
                    </MudSelect>
                </MudItem>
                <MudItem xs="12" md="6" lg="6">
                    <MudNumericField Label="اجرت" @bind-Value="Model.Wage" For="@(() => Model.Wage)" Variant="Variant.Outlined" Margin="Margin.Dense" Step="1" />
                </MudItem>
                <MudItem xs="12" md="6" lg="6">
                    <MudSelect T="ProductTypeVm" Label="نوع جنس" @bind-Value="Model.ProductTypeVm" For="() => Model.ProductTypeVm" Margin="Margin.Dense" Variant="Variant.Outlined">
                        @foreach (var productType in ProductTypeVm.GetProductTypes())
                        {
                            <MudSelectItem T="ProductTypeVm" Value="productType">@productType.Name</MudSelectItem>
                        }
                    </MudSelect>
                </MudItem>
                <MudItem xs="12" md="6" lg="6">
                    <MudSelect T="CaratTypeVm" Label="عیار" @bind-Value="Model.CaratTypeVm" For="() => Model.CaratTypeVm" Margin="Margin.Dense" Variant="Variant.Outlined">
                        @foreach (var caratType in CaratTypeVm.GetCarats())
                        {
                            <MudSelectItem T="CaratTypeVm" Value="caratType">@caratType.Name</MudSelectItem>
                        }
                    </MudSelect>
                </MudItem>
            </MudGrid>

            <MudStack Row Justify="Justify.FlexEnd" Class="mt-5">
                <MudButton OnClick="Cancel" Variant="Variant.Filled" Color="Color.Error">لغو</MudButton>
                <MudButton Color="Color.Primary" ButtonType="ButtonType.Submit" Variant="Variant.Filled" Disabled="@_processing">
                    @if (_processing)
                    {
                        <MudProgressCircular Class="ms-n1" Size="Size.Small" Indeterminate="true" />
                        <MudText Class="ms-2">در حال ذخیره</MudText>
                    }
                    else
                    {
                        <MudText>ذخیره</MudText>
                    }
                </MudButton>
            </MudStack>
        </EditForm>
    </DialogContent>
    <DialogActions>

    </DialogActions>
</MudDialog>
